# しゃべる絵本メーカー v6 実装指示(3Dアート素材の生成とUI高密度化)

以下をそのままClaude Codeに貼り付けてください。
既存リポジトリ(GitHub: Mdouniwa/DOUNIWA、main branch)への作業です。

---

## 【最優先・厳守事項】

- **各ファイル作成/変更後、こまめに `git commit` してください**
- **各フェーズ完了時には必ず `git push origin <ブランチ名>` を実行し、報告してください**
- Playwright/e2eテストの自動実行は行わないでください。ビルド確認は `npm run build` のみ
- 作業ブランチは `main` から新規に切り、最初の応答でブランチ名を明示してください
- 全フェーズ完了後は `main` へマージしてpushしてください
- **GitHub連携によるVercel自動デプロイが有効**なので、mainへのpushで本番反映されます

---

## 背景・今回の課題

現状のUIは「パステルのグラデーション + 絵文字 + テキスト」で構成されており、余白が「洗練」ではなく「スカスカ」に見えている。子ども向けアプリとして期待される情報密度・華やかさに届いていない。

**原因はCSSではなく、イラスト素材が存在しないこと。** 背景・キャラクター・装飾がすべてCSSで代替されているため、画面に「絵」がない。

今回は**3Dレンダリング調のアート素材を一括生成し、それを前提にUIを組み直す。**

## 1. アートディレクション(全素材で統一すること)

- **3Dレンダリング調**。やわらかい質感、あたたかいライティング、被写界深度のあるボケ
- **ファンタジー絵本の世界観**。森・木漏れ日・光の粒・自然のモチーフ
- 色調は明るく彩度高め。ただし毒々しくならないよう、緑・金・淡い青を基調とする
- 子ども向けなので**怖さ・暗さ・鋭さを避ける**

## 2. 「えほんの精」のキャラクター設定(最重要)

**オリジナルの森の妖精キャラクター**として設計する。既存の商業作品のキャラクター名を生成プロンプトに含めてはならない(著作権上の問題があり、生成APIのガードにも抵触する)。以下の要素で組み立てること。

- **若い女性の妖精**。リアル寄りの頭身(6〜7頭身程度)
- 長いブロンドの髪、エルフのような尖った耳、澄んだ大きな瞳
- **清楚で品のある佇まい**。やさしく穏やかな表情
- **衣装は露出を抑えた、上品で健全なデザイン**(淡い緑と白基調のローブ風。子ども向けアプリの顔として適切であること)
- 背中に薄く透ける羽。まわりに光の粒
- 背景は森の木漏れ日。緑と金色の光

**表情バリエーション(5種)**:
`normal`(やさしく微笑む) / `happy`(よろこぶ) / `thinking`(考える・首をかしげる) / `surprised`(おどろく) / `cheer`(応援する・手を挙げる)

**生成方法**: 1枚目(`normal`)を基準画像として生成し、**残り4枚は基準画像を参照画像として渡して生成**する(Nano Banana 2の参照画像機能)。全表情で同一人物に見えることが必須。

**出力**: `public/fairy/fairy-{normal,happy,thinking,surprised,cheer}.png`(既存のプレースホルダーを上書き)。**背景透過が望ましい**が、不可なら単色背景で生成してから背景除去処理を入れること。

## 3. その他の生成素材

### 3-1. ロゴ
- `public/art/logo.png`
- 「しゃべるえほん」の**立体的な描き文字ロゴ**。3D調、金の縁取り、光沢
- 背景透過

### 3-2. 背景アート
- `public/art/bg-home.png` — ホーム画面用。森と空、木漏れ日、遠景に虹
- `public/art/bg-talk.png` — 対話画面用。上記と同じ世界観で、キャラクターが映えるよう中央がやや明るい
- 横長(1920x1080程度)。**スマホの縦画面でも破綻しないよう、重要な要素を中央に寄せる**

### 3-3. 装飾イラスト(10〜14点)
画面の余白に散りばめる小物。すべて**背景透過PNG**、`public/art/deco/` に配置。

動物系: ぞう / うさぎ / くま / ことり / きつね
乗り物系: きしゃ / ききゅう / ふね
自然系: きのこ / おおきな木 / ほし / にじ / くも / はな

### 3-4. UIパーツ
- `public/art/frame-book.png` — 絵本カード用の装飾フレーム(木や蔦のモチーフ、9-slice可能な設計が望ましい)
- `public/art/button-plate.png` — ボタンの立体プレート(木製の看板風)

## 4. 生成スクリプト

`scripts/generate-art.mjs` を新規作成すること。

- 既存の `scripts/generate-fairy.mjs` を参考にする(参照画像方式の実装が既にある)
- `GEMINI_API_KEY` 環境変数で認証(AI Studio の APIキー方式でよい。**Vertex AIは不要** — 素材生成はプロンプトのみで個人情報を含まないため)
- モデルは Nano Banana 2 系(`gemini-3.1-flash-image`)。環境変数 `IMAGE_MODEL` で差し替え可能に
- **1回の実行で全素材を生成**できること。ただし途中失敗時に既存ファイルを壊さないよう、一時ディレクトリに出力してから移動する方式にすること
- **既に生成済みのファイルはスキップする**オプション(`--only=fairy` のような部分実行)を用意すること。再生成のたびに全部作り直すとコストが無駄になる
- 生成した画像は**適切にリサイズ・圧縮**してからリポジトリに入れること(1枚あたり200KB以下を目安。PWAのprecache対象なので肥大化を避ける)

**生成後、実際にスクリプトを実行して素材を作り、リポジトリにコミットすること。** APIキーは道庭さんが環境変数で渡す。キーが未設定の場合はその旨を報告し、スクリプトの作成までで止めること。

## 5. UIの作り直し

素材が揃った前提で、以下を実施する。

### 5-1. ホーム画面
- **背景アートを敷く**(bg-home.png)
- **ロゴ画像に差し替え**(現在のテキスト+📖絵文字をやめる)
- **絵本が0冊のときの空状態を作り込む** — 現在は巨大な点線の空箱で寂しい。装飾イラストとえほんの精を配置し、「はじめての えほんを つくろう!」と誘う画面にする
- 絵本カードに装飾フレームを適用
- **画面の四隅・余白に装飾イラストを散りばめる**(タップ領域を邪魔しない位置に)

### 5-2. 対話画面
- **背景アートを敷く**(bg-talk.png)
- えほんの精を大きく配置(生成した3D素材)
- 質問文の吹き出しを立体的なデザインに
- マイクボタン・選択肢ボタンを**立体的なプレート**に(button-plate.pngまたはCSSでの3D表現)
- 🌸の進捗表示も装飾を強化

### 5-3. その他画面
生成中・確認・表紙設定・再生・親モードも、同じ世界観で統一すること。

### 5-4. レイアウト崩れの修正(重要)
**iPhoneの縦画面でレイアウトが崩れているとの報告がある。** 以下を必ず確認・修正すること。

- iPhone SE(375px)〜iPhone Pro Max(430px)の幅で全画面が破綻しないこと
- セーフエリア(ノッチ・ホームバー)を正しく回避していること
- 背景アートが縦画面で不自然に引き伸ばされないこと(`object-fit: cover` 等で対応)
- 装飾イラストが小さい画面では自動的に減る・隠れる設計にすること(情報密度は上げつつ、操作を邪魔しない)
- 横画面(landscape)でも最低限破綻しないこと

## 6. 守るべき制約

- **タップ領域は最小88px**を維持(装飾を増やしても操作性を落とさない)
- **文字が読めなくても操作できる**設計を維持
- 既存機能(IndexedDB、オフライン再生、効果音、親モード、ページめくり、対話ロジック)を壊さないこと
- **PWAのprecacheサイズに注意**。素材追加で肥大化するので、圧縮を徹底し、必要なら背景アートだけ遅延読み込みにする
- 生成した素材はすべてリポジトリにコミットする(**ランタイムでのAPI呼び出しはゼロにする**)

## 7. 実装フェーズ

1. **Phase 1: 生成スクリプト作成** — `scripts/generate-art.mjs`、部分実行オプション、圧縮処理 → commit & push
2. **Phase 2: 素材生成の実行** — APIキーを受け取って実行、全素材を生成・圧縮・コミット → commit & push
3. **Phase 3: ホーム画面の刷新** — 背景・ロゴ・空状態・カードフレーム・装飾配置 → commit & push
4. **Phase 4: 対話画面の刷新** — 背景・精の大型配置・立体ボタン・吹き出し → commit & push
5. **Phase 5: 残り画面の統一** — 生成中・確認・表紙・再生・親モード → commit & push
6. **Phase 6: レスポンシブ修正** — iPhone実機幅での破綻を全画面で潰す → commit & push
7. **Phase 7: 仕上げ** — precacheサイズ確認、`npm run build`、README更新(素材生成手順の記載) → commit & push → mainへマージ

## 8. 各フェーズ末のセルフチェック(必須)

1. 画面に**十分な情報密度**があり、スカスカに見えないか
2. 2歳・5歳が**文字を読まずに操作できる**か
3. **iPhone縦画面(375px幅)で破綻していない**か
4. タップ領域88px以上が維持されているか
5. `npm run build` が通り、precacheサイズが極端に増えていないか

## 9. 最終報告に含めること

- 生成した素材の一覧とファイルサイズ合計
- 素材生成にかかった概算コスト
- precacheサイズの変化(before/after)
- 再生成が必要になった場合のコマンド

---

以上。まずブランチ名を明示し、Plan Modeで設計(生成プロンプト案、素材リスト、画面ごとのレイアウト方針)を提示してください。
