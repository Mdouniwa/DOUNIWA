# しゃべる絵本メーカー v5 実装指示(対話型への全面再設計)

以下をそのままClaude Codeの初回メッセージとして貼り付けてください。
既存リポジトリ(GitHub: Mdouniwa/DOUNIWA、main branch)に対する大規模な再設計です。

---

## 【最優先・厳守事項】

- **各ファイル作成/変更後、こまめに `git commit` してください**
- **各フェーズ完了時には必ず `git push origin <ブランチ名>` を実行し、GitHub上への反映を出力で明示的に報告してください**
- push完了の報告なしに次のフェーズへ進まないでください
- Playwright/e2eテストの自動実行は行わないでください。ビルド確認は `npm run build` のみで十分です
- 作業ブランチは `main` から新規に切ってください。最初の応答でブランチ名を明示してください
- 全フェーズ完了後は `main` へマージしてpushしてください
- **今回は全フェーズを一気に実装してください。**ただし各フェーズ末に後述の「セルフチェック」を必ず実施し、その結果を出力に含めてください

## 【各フェーズ末のセルフチェック(必須)】

各フェーズを終えるたび、以下を自問して結果を出力に明記してください。**1つでもNGがあれば次に進まず、そのフェーズ内で修正してください。**

1. このフェーズの成果物は、2歳と5歳の子どもが**自分で操作できる**ものになっているか(文字が読めなくても進めるか)
2. 見た目は「AIが作ったテンプレート」ではなく、**この絵本アプリのために作られたもの**に見えるか
3. 既存の動いている機能(IndexedDB保存、オフライン再生、効果音、親モード、ページめくり)を壊していないか
4. `npm run build` が通るか
5. エラー時に子どもが困らない導線があるか(大人を呼ばずに済むか)

---

## 背景・なぜ作り直すのか

このアプリは以下の変遷を辿っている。

- v1〜v3: 家族写真を並べて録音する方式 → 「親が用意した素材を並べるだけ」で子どもの主体性がなかった
- v4: アイコンを5個選んでAI生成する方式 → 「大人が用意した選択肢から選ぶ」入力フォーム的な体験で、依然として物足りなかった

**v5では、アプリが子どもに話しかけ、子どもが答えることで絵本ができる対話型にする。**これが本来やりたかった体験である。

## 1. コア体験

「えほんの精」というキャラクターが子どもに5問前後の質問をする。子どもは**声で答えても、画面のアイコンをタップして答えてもよい**。全部答え終わると、その答えを元にした絵本が生成される。

**設計原則(必ず守ること)**:
- 質問は固定せず、**前の答えを受けて次の質問をAIが動的に生成する**(会話として成立させる。2回目以降も飽きさせない)
- **2歳も5歳も同じフローを使う**(旧v3までの「2歳は鑑賞者/5歳は制作者」という非対称設計は完全に廃止する。この家庭では2歳児も会話ができる)
- 声とタップは**常に両方が使える**(どちらかに誘導しない。その時の気分で選べる)
- 質問文は**必ず音声で読み上げる**(文字が読めなくても進めるため)

## 2. 対話フローの骨格

質問の**流れ**は以下を骨格とするが、**実際の質問文はAIが子どもの答えを見て毎回生成する**こと。テンプレート文をそのまま出さない。

1. 主役を決める(誰・何が出てくる)
2. 場所を決める
3. そこで何をしているか
4. 誰と一緒か(子ども自身を登場させる導線)
5. 最後どうなるか(オチ)

会話例(あくまでイメージ。この通りの固定文にしないこと):

```
精: 「こんにちは! きょうは どんな おはなしを つくる?」
子: 「ぞうさん!」
精: 「ぞうさん いいね! ぞうさんは どこに いるのかな?」
子: 「こうえん!」
精: 「こうえんで ぞうさんは なにを して いるの?」
子: 「すべりだいで あそんでる!」
...
```

**会話が脱線した場合の扱い**: 子どもが質問と無関係な返事をしても否定せず、それを物語の要素として拾って次に進むこと。「わからない」「うーん」のような返事の場合は、精が選択肢を提案して助ける。

## 3. 「えほんの精」のキャラクター設定

このアプリの顔となる重要な要素。以下の方向で**オリジナルキャラクターとして**設計すること。

- **小さな妖精の女の子**。手のひらに乗るくらいのサイズ感
- 清楚でやさしい雰囲気。派手さやコケティッシュさはなく、**穏やかで親しみやすい**
- 背中に薄く光る羽。まわりにやわらかい光の粒が漂う
- 色調はパステル系(やわらかい水色・白・淡い黄色など)、絵本の挿絵と調和すること
- 表情豊かで、子どもの答えに反応してよろこぶ・考える・驚くなどの動きがある

**重要**: 既存の商業キャラクター(ディズニー等)に似せる指示は絶対に出さないこと。著作権上の問題があり、また画像生成APIのガードにも抵触する。あくまでオリジナルデザインとして生成すること。

**実装方法**: 精のキャラクター画像は**ランタイムで毎回生成せず、開発時に数パターン(通常・よろこぶ・かんがえる・おどろく など4〜6種)を生成して静的アセットとしてアプリに同梱**すること。これによりランタイムのAPI依存とコストをゼロにできる。生成にはNano Banana系モデルを使い、**1枚目を基準画像として残りを参照画像方式で生成**し、全表情で同一キャラクターに見えるようにすること。

## 4. 技術構成

### 全体像

```
[iPad/iPhone PWA]
    │
    ├─ 静的配信・絵本閲覧 ────→ [Vercel] (既存のまま)
    │
    └─ 対話・生成 ──Tailscale──→ [Mac mini 対話サーバー]
                                        │
                                        └──→ [Vertex AI]
```

### Mac mini 対話サーバー

- **Node.js + Express**(フレームワークは入れない。この規模では不要)
- リポジトリ内に `server/` ディレクトリを新設して実装する
- **Vertex AI SDK** を使用(AI Studio の APIキー方式ではなく Vertex AI を使う。理由: データが学習に使われないのがデフォルトであり、子どもの声を扱う上で適切)
- 認証は Google Cloud のサービスアカウント鍵(JSONファイル)を環境変数でパス指定する方式とする。**鍵ファイル自体は絶対にリポジトリにコミットしないこと**(`.gitignore` に追加)
- Tailscale経由でのアクセスを想定。CORS設定でPWAのオリジンからのアクセスを許可すること
- launchdでの常駐化を想定した設定ファイル例とセットアップ手順をREADMEに記載すること
- **n8n/Difyが同居している前提**なので、使用ポートは明示的に指定し、README に記載すること(既存サービスと衝突しないもの)

### 使用モデル

| 用途 | モデル | 備考 |
|---|---|---|
| 対話・質問生成 | `gemini-3.5-flash-lite` | 低レイテンシ重視。子どもを待たせない |
| 音声理解 | Gemini(音声を直接入力) | 文字起こしを挟まず意図を解釈させる |
| 物語生成 | `gemini-3.6-flash` | 起承転結の構成力が要るのでこちら |
| 挿絵生成 | Nano Banana 2 (`gemini-3.1-flash-image`) | **参照画像方式必須**(下記) |
| 音声合成 | Gemini 3.1 Flash TTS | 感情表現を自然言語で指示可能、日本語高品質 |

**モデル名は変わりやすいため、すべて環境変数で差し替え可能にすること。**また実装前に最新のGemini APIドキュメントを確認し、モデルIDが正しいか検証すること。

### 挿絵の一貫性(v4からの最重要改善)

v4では各ページの挿絵を独立に生成していたため、**主人公の見た目がページごとにブレていた**。これが「AIが作った感」の主要因と考えられる。v5では必ず以下の手順を踏むこと。

1. まず**主人公の基準画像を1枚生成**する
2. その画像を**参照画像として2ページ目以降のリクエストに渡す**
3. 全ページで同じキャラクターが登場する絵本にする

Nano Banana 2 は最大14枚の参照画像・5キャラクターまでの一貫性保持に対応しているので、この機能を活用すること。

## 5. 画面構成

### S1: ホーム(既存流用)

### S2: 対話画面(新規・このアプリの心臓部)
- 上部: えほんの精(表情が切り替わるアニメーション)
- 中央: 現在の質問文(大きな文字。同時に音声読み上げ)
- 下部: **大きなマイクボタン**(押している間だけ録音する押しっぱなし方式。誤操作しにくい) + **選択肢アイコン4個程度**
- 右上: やめる(ホームへ)
- 進捗: 「あと3つ」のように子どもにも分かる残数表示(数字が読めなくても分かる視覚表現を併用)

**エラー処理**:
- 聞き取れなかった場合、精が「もういっかい いってくれる?」と**音声で**聞き返す
- 3回失敗したら、選択肢アイコンを大きく前面に出して自然に誘導する
- マイク権限が拒否されている場合は、最初からタップ方式で進められるようにする

### S3: 生成中画面(既存を改修)
- 子どもが答えた内容が順に浮かび上がるアニメーション(「ぞうさんが…」「こうえんで…」)
- 精が絵本を作っている様子を見せる
- 30秒〜1分かかる想定。**待ち時間を退屈させないことを最優先**に作ること

### S4: できあがり確認(既存ReviewPagesを流用)
- 生成された絵本を1ページずつ確認、自分の声で録り直し、効果音選択

### S5: 表紙設定・保存(既存流用)
### S6: 再生画面(既存流用)
### S7: 親モード(既存流用)

## 6. データモデル

```ts
interface Book {
  id: string;
  title: string;
  coverImageId: string | null;
  pageIds: string[];
  createdAt: number;
  updatedAt: number;
  lastOpenedAt: number;
  conversation?: Array<{ question: string; answer: string }>; // 対話ログ(v4のiconKeywordsを置き換え)
  characterRefImageId?: string; // 主人公の基準画像ID(参照画像方式用)
}

interface Page {
  // v4から変更なし
  id: string;
  bookId: string;
  imageBlob: Blob;
  thumbBlob: Blob;
  captionText: string;
  audioBlob: Blob | null;
  audioMime: string | null;
  soundEffect: SoundEffect | null;
  narrationSource?: 'generated' | 'recorded';
}
```

**既存の絵本データとの互換性を保つこと**(v4以前に作られた絵本が開けなくならないように、新フィールドはすべてオプショナルにする)。

## 7. 既存インフラで維持すること

- IndexedDBのCRUD・トランザクション削除(`db.ts`)
- iOS Safari対応(AudioContextアンロック、`storage.persist()`、PWA設定、セーフエリア)
- 効果音のWebAudio合成(`soundEffects.ts`)と選択UI
- 親モード(削除2段階確認・並び替え・表紙変更・キャプション編集)
- ページめくりアニメーション
- 350msデバウンス、大きなタップ領域(最小88px)などのUX原則
- **絵本の閲覧・再生は引き続き完全オフラインで動作すること**(対話・生成時のみネットワークが必要)

## 8. 廃止するもの

- v4のアイコン選択画面(`IconSelect.tsx`、`icons.ts`)
- Vercel側のサーバーレス関数(`api/generate-book.ts`) — バックエンドはMac miniに集約するため
- `vercel.json` の関数設定

## 9. 実装フェーズ(各フェーズ末に commit + push + セルフチェック)

1. **Phase 1: Mac mini対話サーバー基盤** — `server/` 新設、Express + Vertex AI SDK接続、CORS設定、環境変数設計、launchd設定例、README記載 → commit & push & セルフチェック
2. **Phase 2: 対話ロジック** — 質問生成、音声理解、会話状態管理、脱線・失敗時のリカバリ → commit & push & セルフチェック
3. **Phase 3: えほんの精のアセット生成** — キャラクター4〜6表情を参照画像方式で生成し静的アセット化。生成用スクリプトも `scripts/` に残すこと → commit & push & セルフチェック
4. **Phase 4: 対話画面UI** — マイクボタン(押しっぱなし)、選択肢アイコン、精のアニメーション、質問の音声読み上げ、進捗表示 → commit & push & セルフチェック
5. **Phase 5: 生成パイプライン** — 対話ログ→物語→**参照画像方式での挿絵**→音声合成 → commit & push & セルフチェック
6. **Phase 6: 既存フローとの接続とv4資産の撤去** — 生成中画面改修、ReviewPages接続、旧アイコン選択・Vercel関数の削除 → commit & push & セルフチェック
7. **Phase 7: 仕上げ** — README全面更新(Mac miniサーバーのセットアップ手順、Vertex AI認証手順、ポート番号、launchd設定、実機確認チェックリスト)、`npm run build` 確認 → commit & push、その後mainへマージしてpush

## 10. 検証方法

- `npm run build` が通ることを必須確認とする
- サーバー側も型チェックが通ることを確認
- Playwright等の自動E2Eテストは実行しないこと
- 実際の対話・生成動作の確認は、道庭さんがMac miniでサーバーを起動してから実機で行う

## 11. 最終報告に必ず含めること

- **Mac miniでのサーバー起動手順**(依存インストール、環境変数設定、起動コマンド、launchd常駐化の手順)
- **Vertex AIの認証設定手順**(Google Cloudプロジェクト作成、サービスアカウント作成、鍵ファイルの取得と配置)
- **使用ポート番号**と、既存のn8n/Difyと衝突しないことの確認
- **PWA側から対話サーバーへの接続先URL**をどこで設定するか(Tailscale経由のアドレス)
- **使用したモデル名**とその選定理由
- **1冊あたりの想定コスト概算**
- 各フェーズのセルフチェック結果

---

以上。まずブランチ名を明示した上で、Plan Modeで設計(特に対話サーバーのAPI設計、会話の状態管理方法、精のキャラクター生成プロンプト案、画面遷移)を提示してください。承認後、全フェーズを通して実装してください。
